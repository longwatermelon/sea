let str:byte* = galloc(10, byte);

fn main() -> int {
    str[0] = 77;

    let val0:int = str[0];

    if (val0==77) {
        syscall(60, 111, 0, 0);
    } else {
        syscall(60, val0, 0, 0);
    }
}
