let write_buf:int = 0;

fn write_char(c:int) -> void {
    write_buf = c;
    syscall(4, 1, &write_buf, 1);
}

fn main() -> int {
    let i:int=0;
    while (i<3) {
        let j:int=0;
        while (j<5) {
            j=j+1;
            if (j==3) {
                continue;
            }
            write_char(i+48);
            write_char(j+48);
            write_char(32);
        }
        i=i+1;
    }
    return 0;
}
