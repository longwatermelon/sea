let write_buf:int = 0;

fn write_char(c:int) -> void {
    write_buf = c;
    syscall(4, 1, &write_buf, 1);
}

fn main() -> int {
    let i:int=0;
    while (i<10) {
        if (i==5) {
            break;
        }
        write_char(i+48);
        i=i+1;
    }
    return 0;
}
